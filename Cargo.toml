[workspace]
resolver = "2"
members = [
    "soma-common",
    "soma-substrate",
    "soma-agent",
    "soma-compositor",
    "soma-cli",
    "soma-updater",
]

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
